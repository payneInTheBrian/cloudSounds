<%- include('partials/header') -%>
<body class="back">
<div class="container  my-5 pb-5 rounded">
  <div class="row ">
    <div class="feed py-5 px-5  ">
      <div class=" ">
          <div>
              <!-- <h3 class="text-dark text-center p-5"><strong>Welcome</strong>: <%= user.userName %></h3> -->
              <!-- <h6 class="text-light"><strong>Email</strong>: <%= user.email %></h6> -->
              <!-- <a href="/logout" class="col-3 btn btn-warning">Logout</a> -->
          
            </div>
          <div class="mt-5">
            <h2 class="text-dark mb-5">Add a post</h2>
            
            <form action="/post/createPost" enctype="multipart/form-data" method="POST">
              <div class="mb-3">
                  <label for="title" class="form-label h5 text-dark"><strong>Title</strong></label>
                  <input type="text" class="form-control" id="title" name="title">
              </div>
              <div class="mb-3">
                <label for="caption" class="form-label h5 text-dark"><strong>Caption</strong></label>
                <textarea class="form-control" id="caption" name="caption"></textarea>
              </div>
              <div class="mb-3">
                <label for="imgUpload" class="form-label h5 text-dark"><strong>Song</strong>(.mp3, .wav, .aiff)</label>
                <input type="file" class="form-control" id="imageUpload" name="file">
              </div>
              <button type="submit" class="btn btn-warning" value="Upload"><strong>Submit</strong></button>
            </form>
          </div>
      <!-- </div>
      <div class="col-6">
        <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= posts[i]._id%>">
              <h4  class="text-light "><%= posts[i].title.toUpperCase()%></h4>
                <h5  class="text-light"><%= posts[i].caption%></h5> 
                <audio alt="<%= posts[i].caption%>" src="<%= posts[i].image%>"></audio>
            </a>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
           <a class="btn btn-warning col-2" href="/feed">Feed</a> 
        </div>  
      </div>   -->
    </div>
  </div>
</div>
</div>
<%- include('partials/footer') -%>
</body>